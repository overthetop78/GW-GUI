namespace Hst.Amiga.FileSystems.Pfs3
{
    public enum Pfs3FreeBlockType
    {
        /* keep or free anodes when freeing blocks */
        // enum Pfs3FreeBlockType {keepanodes, freeanodes};
        keepanodes,
        freeanodes
    }
}