<UserControl x:Class="GWGUI.App.Views.Controls.Visualization.ScpDiskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF">
    <Grid>
        <Border Background="#090C10" CornerRadius="8" ClipToBounds="True">
            <skia:SKElement x:Name="Canvas"
                            PaintSurface="Canvas_PaintSurface"
                            MouseWheel="Canvas_MouseWheel"
                            MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                            MouseRightButtonDown="Canvas_MouseRightButtonDown"
                            MouseRightButtonUp="Canvas_MouseRightButtonUp"
                            MouseMove="Canvas_MouseMove"/>
        </Border>

        <Border Margin="10"
                Padding="8,5"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                Background="#CC111820"
                BorderBrush="#4DFFFFFF"
                BorderThickness="1"
                CornerRadius="6">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button x:Name="ZoomOutButton" Width="30" MinHeight="26" Content="−" Click="ZoomOutButton_Click"/>
                    <Button x:Name="ResetZoomButton" MinWidth="58" MinHeight="26" Margin="4,0" Content="100 %" Click="ResetZoomButton_Click"/>
                    <Button x:Name="ZoomInButton" Width="30" MinHeight="26" Content="+" Click="ZoomInButton_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="SurfaceLabel" Foreground="White" FontWeight="SemiBold"/>
                    <TextBlock x:Name="SelectionLabel" Margin="12,0,0,0" Foreground="#CCD8E1EB"/>
                    <ComboBox x:Name="RevolutionSelector"
                              MinWidth="72"
                              Margin="12,0,0,0"
                              Visibility="Collapsed"
                              SelectionChanged="RevolutionSelector_SelectionChanged"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
