<UserControl x:Class="GWGUI.App.Views.Controls.Visualization.VisualizerLegend"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ItemsControl x:Name="LegendItems">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Margin="0,0,3,0" Padding="3,1" Background="#FFF5F7F9"
                        BorderBrush="#FFDDE3E8" BorderThickness="1" CornerRadius="6">
                    <StackPanel Orientation="Horizontal">
                        <Border Width="6" Height="6" CornerRadius="3" Background="{Binding Value}"
                                Margin="0,0,3,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Key}" Foreground="#FF3B4852" FontSize="9"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
