<UserControl x:Class="GWGUI.App.Controls.WriteTabSection" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:GWGUI.App.Controls" xmlns:l="clr-namespace:GWGUI.App.Localization">
    <Grid>
        <Grid.RowDefinitions><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="16">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <controls:WriteAdvancedSection x:Name="AdvancedSection" Grid.Row="0"/>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="390"/></Grid.ColumnDefinitions>
                    <controls:PathSection x:Name="SourceSection" Label="{l:Loc Write.Source}" Text="{Binding Write.SourcePath, Mode=TwoWay}" Margin="0,0,6,12"/>
                    <controls:ProfileSection x:Name="ProfileSection" Grid.Column="1" Margin="6,0,0,12"/>
                </Grid>
                <controls:WriteFormatSection x:Name="FormatSection" Grid.Row="2" Margin="0,0,0,12"/>
            </Grid>
        </ScrollViewer>
        <Button Grid.Row="1" x:Name="ExecuteButton" Content="{l:Loc Common.Execute}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" Margin="16,4,16,12"/>
    </Grid>
</UserControl>
