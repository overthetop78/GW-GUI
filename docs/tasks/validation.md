# Validation finale des médias et du matériel

Cette liste ne contient que le travail encore actif. Les tâches achevées et leurs résultats sont
conservés dans [validation-completed.md](validation-completed.md).

Les corpus à auditer en lecture seule sont exactement `F:\Rétro` pour les disquettes,
`F:\86Box\Isos` pour les CD et images optiques, `C:\Users\overt\Documents\GW GUI\Emulation\HDD`
et `C:\Users\overt\86Box VMs` pour les disques durs. Les fichiers restent à leur emplacement :
l'audit ne les copie, ne les déplace et ne les modifie pas.

`GWGUI.LocalDiskImageTests` reste absent de `GWGUI.sln` et des workflows GitHub. L'utilisateur
lance manuellement les audits dépendant de ses médias après la préparation et la compilation du
projet. Les résultats locaux servent ensuite à ajouter les corrections reproductibles à cette liste.

- [ ] 1. Valider les corpus locaux en lecture seule
  - [ ] 1.3.21 Terminer la préparation piste par piste et l'analyse des formats
    - [x] Rétablir les commandes et les informations SCP indépendamment de la sélection d'une piste
      - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryScpFormatDiagnosisTests.cs` pour mesurer en lecture seule, sur `F:\Rétro\Disquettes\Génération 4\Génération 4 N°49 - Novembre 1992 - Disquette Démo N°6\Génération 4 - Disquette_Demo_N°6.scp`, la géométrie et les preuves exactes de chaque interprétation avant de modifier le classement.
      - [x] Modifier `.codex/config.toml` pour imposer la vérification et l'ajout de `using System.IO;` avant compilation de tout nouveau fichier C# utilisant les API de fichiers, dossiers, chemins ou flux.
      - [x] Modifier `src/GWGUI.App/Views/Controls/Visualization/VisualizerHeaderSection.xaml.cs` et `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` pour afficher et alimenter le sélecteur Flux/formats détectés dès l'ouverture du SCP, sans dépendre d'un clic sur une piste, puis relancer l'analyse complète lors d'un changement de format.
      - [x] Modifier les inspecteurs et le classement SCP concernés sous `src/GWGUI.MediaEngine/Exploration/Scp` pour rejeter les faux positifs géométriques et ne conserver, pour `Génération 4 - Disquette_Demo_N°6.scp`, que les interprétations réellement validées AmigaDOS, IBM PC et Atari ST.
      - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryScpFormatVerificationTests.cs` pour confirmer en lecture seule que cette image expose exactement AmigaDOS, IBM PC 720 et Atari ST 720 après le nouveau classement.
      - [x] Créer `src/GWGUI.MediaEngine/Exploration/Scp/ScpExplorationProgress.cs`, modifier `ScpAutomaticImageExplorer.cs`, `ScpImageExplorationService.cs`, `src/GWGUI.MediaEngine/Exploration/DiskImageExplorer.cs`, `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` et `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml` pour publier l'avancement réel des pistes et des candidats entre 12 % et l'affichage, éviter les décodages identiques et animer la barre entre deux résultats.
      - [x] Modifier `src/GWGUI.App/Views/Controls/Visualization/MediaInspectorPanel.xaml` et `VisualizerTabSection.xaml` pour rendre visibles, dans une zone fixe, les sections déjà produites par `ScpInspectorController` sur la piste SCP sélectionnée.
      - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryScpProgressVerificationTests.cs` pour chronométrer en lecture seule l'image tri-format exacte, confirmer ses trois formats et vérifier que les pistes puis les candidats publient une progression réelle.
      - [x] Modifier les tests ciblés sous `tests/GWGUI.Tests/Interface/VisualizerViews`, `Interface/ExplorerViews` et `MediaEngine/Exploration` pour reproduire l'ouverture sans sélection, le changement de format, le filtrage géométrique, la progression réelle et l'affichage fixe des détails d'une piste, puis modifier `docs/tasks/validation.md` avec leurs résultats et celui du build Debug. Validation du 12 septembre 2026 : 66/66 tests ciblés réussis ; test local exact réussi sur 164 pistes et 16 candidats, avec uniquement AmigaDOS 880 Kio, IBM PC 720 Kio et Atari ST 720 Kio ; build Debug réussi et exécutable produit dans `build/Debug/GW GUI/gwgui.exe`.
    - [ ] Auditer en lecture seule le corpus local de disquettes et corriger ses régressions
      - [x] Restituer le catalogue logique du SCP tri-format dans ses représentations Atari ST et IBM PC
        - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryScpFileSystemDiagnosisTests.cs` pour comparer en lecture seule les blocs, le secteur d'amorçage, la géométrie et le résultat des interprétations Atari ST 720 et IBM PC 720 de `F:\Rétro\Disquettes\Génération 4\Génération 4 N°49 - Novembre 1992 - Disquette Démo N°6\Génération 4 - Disquette_Demo_N°6.scp`. Résultat : les images `.ima` et `.st` voisines sont identiques, leurs trois premières pistes sont marquées `BAD SECTOR`, aucun FAT12 n'est présent et le seul catalogue de fichiers réel est AmigaDOS.
        - [x] Modifier `src/GWGUI.MediaEngine/Exploration/Results/ExploredDiskImage.cs` afin qu'une représentation sectorielle détectée sans catalogue propre conserve le catalogue logique reconnu de la même capture au lieu de fabriquer des dossiers de pistes physiques.
        - [x] Modifier `tests/GWGUI.Tests/MediaEngine/Exploration/MediaExplorerTests.cs` avec une régression autonome vérifiant qu'une représentation physique alternative sans système de fichiers conserve le catalogue logique reconnu de la capture.
        - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryScpFileSystemVerificationTests.cs` pour confirmer en lecture seule que les choix Atari ST 720 et IBM PC 720 de l'image exacte affichent le catalogue AmigaDOS et ses fichiers. Validation du 12 septembre 2026 : le catalogue `CCIII` et ses entrées sont identiques dans les trois choix AmigaDOS, Atari ST 720 et IBM PC 720.
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` et les tests ciblés du Visualiseur pour remettre la vue Flux à son état initial puis republier ses pistes dans l'ordre lors du retour depuis une représentation sectorielle. Validation ciblée : 1/1 réussie.
        - [x] Créer un contrôle commun sous `src/GWGUI.App/Views/Controls/Common` et modifier `src/GWGUI.App/Views/Windows/Shell/MainWindow.xaml.cs`, `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs`, les bandeaux Explorateur/Visualiseur et leurs tests ciblés afin qu'ils partagent la présentation du média, la carte de progression de reconnaissance et son état de détection, et qu'un choix de format dans l'un mette à jour l'autre sans boucle ni double reconstruction ; conserver Flux et les révolutions uniquement dans le Visualiseur. Validation ciblée : 30/30 scénarios Visualiseur/Explorateur réussis.
        - [x] Modifier `docs/architecture/media-format-orchestration.md` avec les familles de disquettes multiformat attestées par des sources techniques, puis modifier l'ordre des candidats SCP sous `src/GWGUI.MediaEngine` pour examiner d'abord les familles compatibles avec la géométrie observée et conserver le parcours complet en repli. Les sources confirment la compatibilité 720 Kio IBM PC/Atari ST et CrossDOS, sans preuve suffisante pour ajouter Apple au groupe tri-format prioritaire ; 40/40 tests SCP et reconnaissance réussis.
        - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryGeneration4Disk5DiagnosisTests.cs` pour localiser la disquette Génération 4 Démo n°5 sous `F:\Rétro`, analyser en lecture seule toutes ses pistes et révolutions, comparer ses images voisines et mesurer séparément la reconnaissance AmigaDOS, les fichiers attendus et les autres formats physiques. Résultat : AmigaDOS restitue le volume `POVRAY` et 36 entrées depuis 1 470/1 760 blocs ; les zones IBM PC/Atari ST occupent 26 pistes complètes sur les cylindres 41 à 53.
        - [x] Modifier `src/GWGUI.MediaEngine/Exploration/Interpretation/FileSystemAlternativePolicy.cs`, `src/GWGUI.MediaEngine/Exploration/Scp/ScpCandidateRanker.cs`, `src/GWGUI.MediaEngine/Exploration/Scp/ScpExplorationThresholds.cs` et `tests/GWGUI.Tests/MediaEngine/Exploration/MediaExplorerTests.cs` pour conserver un catalogue reconnu avec des entrées contrôlées malgré des données de fichiers manquantes, et reconnaître une zone IBM PC/Atari ST 720 Kio appuyée par au moins 20 pistes complètes sur une capture 80 pistes double face, sans accepter les familles ou géométries contradictoires. Validation ciblée : 3/3 scénarios réussis.
        - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryGeneration4Disk5VerificationTests.cs` pour confirmer en lecture seule les formats exacts et les fichiers réellement visibles de la disquette Génération 4 Démo n°5 après correction. Validation du 12 septembre 2026 : AmigaDOS 880 Kio, Atari ST 720 Kio et IBM PC 720 Kio uniquement ; volume `POVRAY`, `amiga.doc` et `dat2pov` visibles ; 1/1 réussi en 57 secondes.
        - [x] Modifier `tests/GWGUI.Tests/Interface/VisualizerViews/MediaVisualizationLayoutTests.cs` pour vérifier la position fixe du nouveau contrôle d'identité commun au lieu de rechercher le nom interne `FileName` à travers une frontière de portée XAML. Validation ciblée Visualiseur, Explorateur, classement SCP et reconnaissance : 75/75 scénarios réussis.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats des tests ciblés et du build Debug, après suppression des tests temporaires et vérification de `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : 75/75 tests ciblés réussis, test local n°5 réussi, aucun test temporaire restant et build Debug officiel réussi ; exécutable vérifié dans `build/Debug/GW GUI/gwgui.exe`.
      - [x] Corriger la fausse reconnaissance et les plantages observés sur Génération 4 Démo n°5
        - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryGeneration4Disk5ContentDiagnosisTests.cs` pour comparer en lecture seule les formats réellement prouvés, les noms, tailles, indicateurs de validité et contenus octet par octet des fichiers du SCP avec l'ADF voisin, puis consigner les écarts dans `docs/tasks/validation.md` : le SCP prouve uniquement `amiga.amigados`; 30 fichiers sur 31 sont identiques octet par octet à l'ADF; `dat2pov.doc` est partiel dans le SCP (3 522 octets contre 4 010 dans l'ADF pour 4 498 déclarés); `dat2pov` est identiquement partiel dans les deux images (3 904 octets pour 13 180 déclarés); le lecteur renseigne le contenu récupérable mais laisse `DataValid` indéfini.
        - [x] Modifier `src/GWGUI.MediaEngine/Exploration/Scp/ScpCandidateRanker.cs`, `src/GWGUI.MediaEngine/Exploration/Scp/ScpExplorationThresholds.cs` et `tests/GWGUI.Tests/MediaEngine/Exploration/MediaExplorerTests.cs` pour retirer la déduction IBM PC/Atari ST fondée uniquement sur une zone MFM partielle et n'exposer que les formats démontrés par une structure propre au format ou un système de fichiers reconnu.
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageCancellationScope.cs`, `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` et les tests ciblés sous `tests/GWGUI.Tests/Interface/VisualizerViews` et `Interface/ExplorerViews` pour donner à chaque changement de représentation son jeton vivant jusqu'à la fin, absorber l'annulation attendue et empêcher `TaskCanceledException` et `ObjectDisposedException` d'atteindre le dispatcher.
        - [x] Modifier `src/GWGUI.MediaEngine/FileSystems/Amiga/AmigaDosDirectoryReader.cs` et les scénarios AmigaDOS sous `tests/GWGUI.Tests/Media/FileSystems` pour conserver séparément la validité du bloc d'en-tête dans `MetadataValid`, renseigner `DataValid` depuis la chaîne de blocs du fichier et conserver les octets effectivement récupérés lorsqu'un fichier est partiel.
        - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryGeneration4Disk5ContentVerificationTests.cs` pour confirmer en lecture seule que le SCP n'expose que le format prouvé `amiga.amigados`, que ses 31 fichiers ont un contenu réellement récupéré et que les deux fichiers incomplets sont signalés par `DataValid=false`.
        - [x] Modifier `tests/GWGUI.Tests/MediaEngine/Exploration/MediaExplorerTests.cs` pour vérifier que l'exploration réemploie la représentation déjà lue tout en autorisant le document enrichi par le volume direct détecté, sans exiger à tort la même instance de document.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats corrigés, exécuter les tests ciblés et `scripts/build.ps1 -Configuration Debug`, puis vérifier `build/Debug/GW GUI/gwgui.exe` : 168 tests ciblés réussis, vérification locale n°5 réussie, build Debug réussi et exécutable présent.
      - [x] Restituer les fichiers propres aux représentations Atari ST et IBM PC de Génération 4 Démo n°6
        - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryGeneration4Disk6CatalogDiagnosisTests.cs` pour comparer en lecture seule les catalogues, secteurs d'amorçage et images voisines Atari ST/IBM PC de la disquette exacte sous `F:\Rétro`, et déterminer pourquoi le catalogue AmigaDOS est réutilisé. Résultat : le SCP conserve 612 secteurs IBM/Atari des cylindres 3 à 36, mais les cylindres 0 à 2 contenant l'amorce, les FAT et le répertoire racine ont été réécrits ; les chaînes 8.3 restantes ont des clusters hors plage et ne constituent pas un catalogue récupérable.
        - [x] Modifier les lecteurs ou la sélection de documents concernés sous `src/GWGUI.MediaEngine` afin que les choix Atari ST et IBM PC affichent leurs propres fichiers quand leur système de fichiers est réellement reconnu. `ExploredDiskImage.SelectFormat` sélectionne désormais uniquement le volume associé au format demandé et ne greffe plus le catalogue AmigaDOS sur une image sectorielle IBM/Atari sans système de fichiers reconnu.
        - [x] Modifier les tests ciblés sous `tests/GWGUI.Tests/MediaEngine/Exploration` pour interdire la substitution d'un catalogue AmigaDOS à une représentation Atari ST ou IBM PC possédant son propre catalogue. Le scénario `PhysicalAlternativeDoesNotBorrowAnotherFormatsLogicalCatalog` réussit : le format sélectionné conserve sa propre image et aucun volume AmigaDOS n'est réutilisé.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats de la vérification locale, puis supprimer le test temporaire. La vérification locale confirme 612 secteurs propres aux interprétations Atari ST et IBM PC, limités aux cylindres 3 à 36, sans catalogue FAT12 récupérable ; le test temporaire a été supprimé.
      - [x] Empêcher les annulations attendues du chargement partagé d'atteindre le dispatcher WPF
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` pour absorber l'annulation d'une présentation sectorielle remplacée pendant `LoadAsync` et ne conserver que le résultat de l'opération courante.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/VisualizerViews` pour reproduire le remplacement de la présentation pendant un chargement partagé sans `TaskCanceledException` ni `ObjectDisposedException`. Le scénario `ReplacedSectorPresentationDoesNotEscapeCancellation` réussit.
      - [x] Relier l'ouverture d'une image entre le Visualisateur et l'Explorateur
        - [x] Modifier `src/GWGUI.App/Views/Windows/Shell/MainWindow.xaml.cs` pour faire passer l'ouverture depuis le Visualisateur et les actions de fin de lecture par `DiskImageWorkspaceController.LoadAsync`, tout en gardant l'onglet demandé actif et les dossiers de dialogue indépendants.
        - [x] Modifier `tests/GWGUI.Tests/Interface/VisualizerViews/VisualizerDocumentScenarios.cs` pour vérifier que le chargement commun peuple réellement l'Explorateur et le Visualisateur avec la même analyse. Le scénario vérifie le chemin, le dossier racine et la représentation Flux avant de contrôler la sélection synchronisée des formats.
        - [x] Modifier `docs/tasks/validation.md` avec le résultat du test ciblé et du build Debug, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Les trois scénarios ciblés réussissent ensemble et `scripts/build.ps1 -Configuration Debug` termine avec l'exécutable `build/Debug/GW GUI/gwgui.exe`.
      - [x] Corriger l'ouverture sans résultat depuis les deux boutons du Visualisateur
        - [x] Modifier `docs/tasks/validation.md` avec l'exception exacte du nouvel essai lue dans les logs et le point d'arrêt réel du chargement commun. Aucun nouveau journal d'erreur n'a été produit après 14:30 : `LoadAsync` attend séquentiellement la fin complète de `LoadExplorerAsync`, dont la progression reste confinée à l'Explorateur, avant d'appeler le Visualisateur.
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` et les contrôles concernés pour vider immédiatement l'ancien document, construire le nouveau rendu SCP pendant le scan commun, publier le même avancement dans la carte du Visualisateur, les deux barres de pistes et la barre d'état, puis afficher le résultat mémorisé dans les deux onglets.
        - [x] Modifier le câblage sous `src/GWGUI.App` pour que le bouton supérieur et le bouton central utilisent exactement la même action d'ouverture et gardent le Visualisateur actif.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/VisualizerViews` pour reproduire l'ouverture par chacun des deux boutons, vérifier les sorties de progression et interdire un retour silencieux sans document. Les 27 scénarios du Visualisateur réussissent ensemble.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats du test ciblé et du build Debug, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Validation finale du 12 septembre 2026 : les 85 scénarios ciblés MediaEngine, Explorateur et Visualisateur réussissent ; `scripts/build.ps1 -Configuration Debug` termine correctement et l'exécutable `build/Debug/GW GUI/gwgui.exe` est présent.
      - [x] Vérifier les catalogues Atari ST et IBM PC supposés présents sur la disquette Génération 4 n°6
        - [x] Créer puis supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryScpFatCatalogDiagnosisTests.cs` pour relever en lecture seule les secteurs FAT, répertoires et fichiers décodables pour les interprétations Atari ST 720 et IBM PC 720 de `F:\Rétro\Disquettes\Génération 4\Génération 4 N°49 - Novembre 1992 - Disquette Démo N°6\Génération 4 - Disquette_Demo_N°6.scp`. Résultat confirmé : les deux représentations contiennent 612 blocs sur 1 440 et commencent au bloc logique 54 ; les trois premiers cylindres comprenant l'amorce, les FAT et le répertoire racine sont absents.
        - [x] Modifier `docs/tasks/validation.md` pour classer sans objet la création de catalogues Atari ST ou IBM PC pour cette capture : ni le SCP ni ses variantes ST/IMA ne contiennent un système de fichiers FAT12 récupérable, et l'Explorateur doit conserver un volume vide au lieu d'inventer des fichiers ou de reprendre ceux d'AmigaDOS.
      - [x] Garder l'Explorateur lisible pendant la reconnaissance commune
        - [x] Modifier `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml` et `ExplorerSection.xaml.cs` pour conserver le chemin, le bandeau et la zone d'exploration visibles pendant le scan, puis remplir les résultats au fur et à mesure lorsqu'ils deviennent disponibles.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/ExplorerViews` pour vérifier que le chargement ne masque plus toute la surface de l'Explorateur. Validation ciblée : 7/7 scénarios réussis.
      - [x] Afficher les détails d'une piste choisie dans la barre inférieure du Visualisateur
        - [x] Modifier les contrôles concernés sous `src/GWGUI.App/Views/Controls/Visualization` pour que le clic sur une case de la barre inférieure sélectionne la piste et le secteur correspondants dans le disque puis alimente le même panneau de détails qu'un clic direct sur le rendu.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/VisualizerViews` pour vérifier que le clic sur chaque face de la barre inférieure met à jour la piste, le secteur, le résumé et la sélection visible du disque. Validation ciblée : 60/60 scénarios du Visualisateur réussis.
      - [x] Documenter les preuves disponibles pour les SCP ambigus IBM PC et Atari ST
        - [x] Créer `tests/GWGUI.LocalDiskImageTests/TemporaryGeneration4MultiFormatDiagnosisTests.cs` pour comparer en lecture seule les secteurs, catalogues et fichiers des variantes SCP, ST et IMA des disquettes n°53 et n°3 trouvées sous `F:\Rétro\Disquettes`. Les reconstructions explicites Atari ST et IBM PC de n°53 produisent le même FAT12 valide et les mêmes trois fichiers que les variantes ST/IMA ; n°3 fournit également un FAT12 valide pour les deux interprétations.
        - [x] Créer `tests/GWGUI.LocalDiskImageTests/TemporaryFatPlatformEvidenceDiagnosisTests.cs` pour comparer en lecture seule les secteurs d'amorçage, BPB, signatures physiques et taux de reconstruction des SCP `Joystick_40`, Génération 4 n°3, n°53, Push Over et n°37, sans déduire la plateforme des noms, extensions ou contenus des fichiers. Résultat : Joystick, n°3 et Push Over partagent une amorce DOS et une géométrie FAT12 720 Kio compatibles avec IBM PC comme Atari ST ; ces données seules ne permettent pas d'ajouter Atari ST aux deux derniers tout en l'excluant de Joystick.
        - [x] Créer `tests/GWGUI.LocalDiskImageTests/TemporaryJoystick40RegressionTests.cs` et confirmer en lecture seule que `Joystick_40.scp` conserve exactement AmigaDOS et IBM PC, avec les deux catalogues distincts montrés par l'utilisateur. Validation locale : test réussi en 54 secondes.
        - [x] Créer `tests/GWGUI.LocalDiskImageTests/TemporaryTiltTriFormatRegressionTests.cs` et inventorier les SCP Tilt n°105, n°110, n°111 et n°117. Résultat : n°105 et n°110 exposent actuellement AmigaDOS et IBM PC ; n°111 et n°117 seulement AmigaDOS ; chaque analyse dure de 55 à 59 secondes. Des variantes ADF, IMA et ST existent pour n°105, n°110 et n°111, tandis que n°117 n'a qu'une variante ADF.
      - [x] Recalculer le rendu Flux quand la révolution change
        - [x] Modifier `src/GWGUI.App/Rendering/Scp/SkiaScpRenderer.cs` pour appliquer la révolution déjà préparée et choisie à toutes les pistes affichées ; `VisualizerTabSection.xaml.cs` et `ScpDiskView.xaml.cs` transmettent déjà ce choix global et invalident les deux faces.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/VisualizerViews` pour vérifier que les trois révolutions modifient réellement le rendu d'une piste non sélectionnée et que Toutes les révolutions conserve la synthèse. Validation ciblée : 61/61 scénarios du Visualisateur réussis.
      - [x] Permettre le choix manuel d'un format non détecté
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/DiskClassificationSelector.xaml.cs`, `ExplorerSection.xaml.cs` et `DiskImageWorkspaceController.cs` pour donner la priorité au choix avancé, désactiver automatiquement la détection automatique, garder le panneau accessible pendant l'analyse et lancer une exploration explicite lorsque le format choisi n'est pas en mémoire.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/ExplorerViews` et `tests/GWGUI.Tests/Interface/VisualizerViews` pour vérifier la sélection manuelle d'un format absent de la détection et son analyse explicite. Validation ciblée : 73/73 scénarios Explorateur et Visualisateur réussis.
      - [x] Réduire et rendre lisible le temps de reconnaissance SCP
        - [x] Recréer `src/GWGUI.MediaEngine/Recognition/Scp/ScpFamilyProbe.cs`, `ScpFamilyProbeCatalog.cs`, `ScpFamilyProbeDefinition.cs` et `ScpTrackSampler.cs` pour sonder rapidement des pistes réparties sur toute la capture, retenir toutes les familles ayant livré un marqueur de secteur valide et publier chaque famille testée ainsi que son résultat sans examiner les noms ou contenus de fichiers.
        - [x] Modifier `src/GWGUI.MediaEngine/Composition/MediaEngineFactory.cs`, `src/GWGUI.MediaEngine/Decoding/Scp/Sectors/ScpSectorImageCandidate.cs`, `src/GWGUI.MediaEngine/Exploration/Scp/ScpAutomaticImageExplorer.cs` et `ScpExplorationProgress.cs` pour exécuter cette sonde avant la reconstruction complète, conserver simultanément toutes les familles prouvées d'une disquette multiformat et signaler séparément la sonde, le nom lisible de chaque format testé et la lecture de son catalogue.
        - [x] Modifier `src/GWGUI.MediaEngine/Composition/ScpSectorDecodingComposition.cs`, `src/GWGUI.MediaEngine/Reconstruction/AmigaScpSectorImageReader.cs` et les décodeurs concernés pour publier l'avancement piste et révolution à l'intérieur de chaque candidat sélectionné par la sonde.
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` et les traductions communes sous `src/GWGUI.App/Resources` pour afficher dans la carte et la barre d'état l'opération, chaque format testé, son résultat, la piste, la révolution et un pourcentage continu au lieu du seul identifiant technique du candidat.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/MediaEngine/Exploration` et `tests/GWGUI.Tests/Interface/VisualizerViews` pour vérifier l'élagage des faux candidats, la progression interne et l'absence de blocage visuel à 42–43 %. Validation du 12 septembre 2026 : 83/83 scénarios ciblés réussis ; `Joystick_40.scp` conserve exactement AmigaDOS et IBM PC avec leurs catalogues distincts en 19 s au lieu de 54–59 s ; les Tilt Amiga seules prennent 5–7 s et les Tilt Amiga + MFM 21–24 s, sans lancer les familles Apple, Commodore ou DEC non prouvées.
      - [x] Publier la progression commune pour toutes les images ouvertes
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` pour alimenter la carte de reconnaissance, les indicateurs inférieurs et la barre d'état pendant la lecture et l'exploration des images sectorielles, ADF et autres formats, depuis le Visualisateur comme depuis l'Explorateur.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/ExplorerViews` et `tests/GWGUI.Tests/Interface/VisualizerViews` pour vérifier la progression commune des sources SCP et non-SCP dans les deux onglets. Validation ciblée : 74/74 scénarios réussis ; la progression non-SCP est visible simultanément dans l'Explorateur, la carte du Visualisateur et la barre d'état, puis disparaît à la fin du chargement.
      - [x] Corriger Génération 4 n°53 et classer sans objet pour l'instant les interprétations Atari ST ambiguës de n°3 et des Tilt
        - [x] Créer `tests/GWGUI.LocalDiskImageTests/TemporaryScpPlatformStructureDiagnosisTests.cs` pour comparer uniquement la géométrie, l'ordre et la position des secteurs, les révolutions et les structures de flux des SCP n°3, n°53, `Joystick_40` et Tilt. Résultat : aucune marque IAM n'est présente ; n°3 et `Joystick_40` partagent les mêmes structures ISO MFM et le même en-tête DOS, sans preuve physique permettant de déclarer Atari ST pour l'une seulement.
        - [x] Sans objet pour l'instant — ne pas modifier `tests/GWGUI.LocalDiskImageTests/TemporaryScpPlatformStructureDiagnosisTests.cs` pour approfondir une distinction absente des secteurs : la comparaison directe en lecture seule confirme que les variantes ST et IMA de n°3, Tilt n°105 et Tilt n°110 sont identiques octet par octet et que TOS monte leur BPB FAT12 commun sans identifiant de plateforme.
        - [x] Modifier `src/GWGUI.MediaEngine/Exploration/Scp/ScpAutomaticImageExplorer.cs` et `ScpCandidateRanker.cs` pour normaliser la géométrie logique 80 pistes avant l'inspection FAT12 et restituer les trois fichiers propres aux interprétations Atari ST et IBM PC réellement reconnues de n°53, sans déduire la plateforme des noms, extensions ou contenus.
        - [x] Modifier `tests/GWGUI.Tests/MediaEngine/Exploration/MediaExplorerTests.cs` avec une régression autonome de la normalisation d'une FAT12 720 Kio capturée sur 82 pistes, puis vérifier localement n°53 et `Joystick_40`. Validation : n°53 restitue AmigaDOS, Atari ST et IBM PC avec ses trois fichiers FAT12 ; `Joystick_40` conserve exactement AmigaDOS et IBM PC avec leurs catalogues distincts ; 85/85 scénarios ciblés réussis.
        - [x] Sans objet — aucune règle fiable ne distingue IBM PC et Atari ST lorsque leurs secteurs et leur FAT12 sont identiques ; conformément au choix du 12 septembre 2026, laisser de côté les cas tri-format ambigus n°3 et Tilt plutôt que d'utiliser leurs noms ou contenus.
        - [x] Supprimer `tests/GWGUI.LocalDiskImageTests/TemporaryGeneration4MultiFormatDiagnosisTests.cs`, `TemporaryFatPlatformEvidenceDiagnosisTests.cs`, `TemporaryJoystick40RegressionTests.cs`, `TemporaryTiltTriFormatRegressionTests.cs` et `TemporaryScpPlatformStructureDiagnosisTests.cs` après validation des corrections permanentes.
      - [x] Corriger la première image ATR Atari 400-800 vérifiée dans `F:\Rétro\A Trier`
        - [x] Modifier `docs/tasks/validation.md` pour consigner le diagnostic de `8bit Mouse, The v2.01 (19xx)(Broomfield, Graham - Hunt, Colin).atr` : l'en-tête ATR déclare correctement 92 160 octets, soit 720 secteurs de 128 octets et 90 Kio ; le lecteur adresse toutefois chaque secteur comme un cylindre, ce qui produit 720 fausses pistes au lieu de 40 pistes de 18 secteurs. Le VTOC Atari DOS est valide et le catalogue contient des entrées actives, dont plusieurs fichiers vides légitimes rejetés par la validation actuelle.
        - [x] Modifier `src/GWGUI.MediaEngine/Formats/Floppy/Atr/AtrLayout.cs` et `AtrReader.cs` pour attribuer aux formats Atari 90, 130 et 180 Kio leur géométrie réelle et adresser chaque secteur par cylindre, face et numéro dans la piste.
        - [x] Modifier `src/GWGUI.MediaEngine/FileSystems/Atari/Dos/AtariDosDirectoryReader.cs` pour accepter une entrée active vide lorsque son compteur et son premier secteur valent tous deux zéro, tout en rejetant une entrée dont une seule de ces deux valeurs est nulle.
        - [x] Modifier `tests/GWGUI.Tests/Media/ImageContainers/AtariContainerScenarios.cs`, `ImageContainersTests.cs`, `tests/GWGUI.Tests/Media/FileSystems/AtariFileSystemScenarios.cs` et `FileSystemsTests.cs` pour vérifier les géométries ATR standards, les adresses physiques, les fichiers Atari DOS vides et le catalogue non vide de cette structure.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats des tests ciblés et du build Debug, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : 6/6 tests ATR et Atari DOS ciblés réussis ; le build Debug officiel réussit et produit `build/Debug/GW GUI/gwgui.exe`.
      - [x] Restaurer la barre d'état globale pendant la lecture d'une image
        - [x] Sans objet — ne pas modifier `src/GWGUI.App/Views/Controls/Shell/ApplicationStatusBar.xaml` : sa comparaison avec `HEAD` confirme que le dessin antérieur et la largeur fixe de 125 pixels du libellé sont intacts ; l'encombrement vient uniquement du nom de fichier injecté par le contrôleur dans le texte après la barre.
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` pour conserver la progression globale existante fondée sur le nombre réel de pistes quelle que soit l'origine de la lecture, retirer le détail long ajouté au texte de statut et préserver sa disparition existante à la fin.
        - [x] Modifier `tests/GWGUI.Tests/Interface/VisualizerViews/VisualizerDocumentScenarios.cs` et `VisualizerViewsTests.cs` pour vérifier, dans le scénario partagé avec l'Explorateur, l'affichage global pendant la lecture, le libellé court sans nom de fichier et la disparition de la progression à la fin.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats des tests ciblés et du build Debug, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : 8/8 tests ciblés ATR, Atari DOS et progression partagée réussis ; build Debug officiel réussi ; exécutable vérifié dans `build/Debug/GW GUI/gwgui.exe`.
      - [x] Restaurer la progression par pistes dans la barre d'état lors de la lecture d'une image
        - [x] Modifier `docs/tasks/validation.md` avec la correction demandée après comparaison de `ApplicationStatusBar.xaml` avec les commits `250de59a`, `1eddce0b` et le chargement partagé actuel : la même zone de progression de la barre d'état doit se configurer en une ou deux bandes selon le nombre réel de pistes et de faces du média en cours de lecture.
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` et les fichiers strictement nécessaires de la barre d'état pour alimenter cette même zone avec la géométrie et l'avancement déjà calculés par le Visualisateur, sans nom de fichier ni modification des autres éléments de statut.
        - [x] Modifier les tests ciblés existants sous `tests/GWGUI.Tests/Interface/VisualizerViews` pour vérifier l'adaptation à 40 pistes de l'ATR et aux géométries à deux faces, puis conserver la disparition existante à la fin.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats des tests ciblés et du build Debug officiel, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : 10/10 tests ciblés réussis, dont les géométries sectorielles communes 40 pistes sur une face et 80 pistes sur deux faces avec avancement effectif ; build Debug officiel réussi ; exécutable vérifié dans `build/Debug/GW GUI/gwgui.exe`.
      - [x] Corriger le bandeau commun du Visualisateur et de l'Explorateur
        - [x] Modifier les ressources communes et toutes leurs traductions sous `src/GWGUI.App/Resources` pour traduire les types de média et de représentation affichés dans le bandeau partagé. Les dix libellés communs ont été ajoutés à la base puis traduits par Argos dans les 28 catalogues localisés.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml`, `MediaDocumentIdentity.xaml.cs`, `src/GWGUI.App/Views/Controls/Visualization/VisualizerHeaderSection.xaml.cs` et `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml.cs` pour afficher le nom localisé du format, du type de média et de la représentation au lieu des identifiants internes et des noms d'énumérations.
        - [x] Sans objet — la création de `src/GWGUI.App/Views/Controls/Common/MediaWorkspaceHeader.xaml` et `MediaWorkspaceHeader.xaml.cs` a été abandonnée après comparaison avec les anciens commits et les contrôles existants ; ces fichiers ont été supprimés et le `CardSection` commun a été repris directement par l'action corrective ci-dessous.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Visualization/VisualizerHeaderSection.xaml.cs` et `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml.cs` pour masquer le sélecteur des formats détectés lorsqu'il ne contient qu'un seul choix et conserver son changement réel de représentation lorsqu'il en contient plusieurs.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/DiskClassificationSelector.xaml.cs` pour publier le format choisi après un changement manuel de machine, afin que le chemin de relecture déjà raccordé dans `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` exécute réellement la nouvelle exploration et sa présentation.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml.cs`, `src/GWGUI.App/Views/Controls/Visualization/VisualizerHeaderSection.xaml.cs` et `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml.cs` pour exposer aux scénarios d'interface le `CardSection`, le sélecteur commun et les trois libellés réellement affichés.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml.cs` pour importer l'espace de noms commun requis par `CardSection`, `MediaDocumentIdentity` et `DiskClassificationSelector`.
        - [x] Supprimer `src/GWGUI.App/Views/Controls/Common/MediaWorkspaceHeader.xaml` et `MediaWorkspaceHeader.xaml.cs`, puis modifier `src/GWGUI.App/Views/Controls/Visualization/VisualizerHeaderSection.xaml`, `VisualizerHeaderSection.xaml.cs`, `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml` et `ExplorerSection.xaml.cs` pour reprendre directement le `CardSection` commun déjà utilisé par les autres onglets, avec les commandes propres à chaque écran et le `DiskClassificationSelector` commun dans sa surimpression.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/VisualizerViews` et `tests/GWGUI.Tests/Interface/ExplorerViews` pour vérifier le `CardSection` commun, les libellés localisés, le masquage du choix unique et la publication du changement manuel de format. Les 76 scénarios ciblés passent.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats des tests ciblés et du build Debug officiel, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : 76/76 scénarios Visualisateur/Explorateur réussis ; build Debug officiel réussi ; exécutable vérifié dans `build/Debug/GW GUI/gwgui.exe`.
      - [x] Annuler la correction non demandée du changement manuel de machine
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/DiskClassificationSelector.xaml.cs` pour rétablir la publication existante du premier format lorsqu'une machine est choisie manuellement.
        - [x] Modifier `tests/GWGUI.Tests/Interface/ExplorerViews/ExplorerDocumentScenarios.cs` pour rétablir les attentes correspondant à ce comportement existant.
      - [x] Présenter proprement un format manuel incompatible avec l'image ouverte
        - [x] Modifier `src/GWGUI.App/Views/Dialogs/Common/CommonErrorDialog.cs` et `src/GWGUI.App/Views/Windows/Shell/MainWindow.xaml.cs` pour réutiliser la fenêtre thématisée existante avec une présentation informative dans ce cas précis.
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` et les ressources `Explorer.resx` sous `src/GWGUI.App/Resources` pour présenter un choix manuel incompatible comme un format non pris en charge par l'image ouverte, sans le qualifier d'image invalide ou corrompue.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/ExplorerViews` pour vérifier le message informatif sans modifier le changement manuel de format.
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` pour conserver le format demandé dans la portée du traitement d'échec.
        - [x] Modifier `docs/tasks/validation.md` avec le résultat des scénarios ciblés de la boîte informative. Validation du 12 septembre 2026 : 12/12 scénarios Explorateur réussis ; le changement manuel conserve son fonctionnement existant et un format incompatible ouvre la boîte thématisée avec un message informatif.
      - [x] Corriger la présentation commune du média dans le Visualisateur et l'Explorateur
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml` pour regrouper le nom, le résumé et les trois informations du média sans colonnes fixes créant de grands espaces vides.
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs`, `src/GWGUI.App/Views/Windows/Shell/MainWindow.xaml.cs` et les clés `Explorer.SelectedFormatUnsupportedTitle` et `Explorer.SelectedFormatUnsupported` dans la base et les 28 catalogues `Explorer.resx` sous `src/GWGUI.App/Resources` pour nommer le format incompatible dans le message, relancer Argos puis corriger chaque traduction dont le sens, la langue ou les paramètres `{0}` et `{1}` sont incorrects. Les 28 catalogues localisés et la base contiennent les deux paramètres après vérification.
        - [x] Modifier les catalogues `Common.resx` sous `src/GWGUI.App/Resources` pour remplacer les traductions incorrectes de « Floppy disk » dans les 28 langues, dont « Disquette » en français, puis vérifier chaque valeur. Les 28 valeurs ont été relues après écriture.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml` et `ExplorerSection.xaml.cs` pour ne plus répéter le résumé des formats détectés sous le même bandeau lorsque le choix de format unique est masqué.
        - [x] Modifier `src/GWGUI.App/Localization/Extensions/LocExtension.cs` et les catalogues `Formats.resx` sous `src/GWGUI.App/Resources` pour afficher tous les noms de formats depuis la base commune invariante et retirer leurs traductions locales contradictoires.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/VisualizerViews` et `tests/GWGUI.Tests/Interface/ExplorerViews` pour vérifier la disposition compacte commune, l'absence de résumé répété et les libellés invariants ou traduits attendus. Validation du 12 septembre 2026 : 31/31 scénarios ciblés Visualisateur/Explorateur réussis.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Visualization/VisualizerHeaderSection.xaml.cs`, `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` et le raccordement strictement nécessaire dans `src/GWGUI.App/Views/Windows/Shell/MainWindow.xaml.cs` pour que chaque choix de format du Visualisateur réinterprète l'image courante, y compris lors du retour au format initial.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/VisualizerViews` pour reproduire un choix incompatible puis le retour au format initial et vérifier les deux tentatives de rechargement. Validation du 12 septembre 2026 : les 3 scénarios de changement SCP, synchronisation et changement non-SCP réussissent.
        - [x] Modifier `docs/tasks/validation.md` avec le résultat des scénarios ciblés et du build Debug officiel, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : 78/78 scénarios Visualisateur/Explorateur réussis, 32/32 contrôles de résolution des catalogues dans toutes les langues réussis et build Debug officiel réussi ; exécutable vérifié à `build/Debug/GW GUI/gwgui.exe`. Le test distinct de mise en page longue de `ReadCompletionBanner` conserve 4 échecs `TextWrapping=NoWrap`, hors de ce groupe et sans modification de ce contrôle.
      - [x] Simplifier le bandeau média et réparer les quatre échecs de mise en page signalés
        - [x] Modifier `src/GWGUI.App/Views/Controls/Read/ReadCompletionBanner.xaml.cs` pour donner au résumé de secours le même retour à la ligne que le résumé réellement affiché par `ReadCaptureCard`.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml` et `.xaml.cs` pour retirer les trois libellés redondants de format, type de média et représentation, et conserver uniquement l'icône, le nom du fichier et le résumé détecté.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml.cs` et `src/GWGUI.App/Views/Controls/Visualization/VisualizerHeaderSection.xaml.cs` pour utiliser ce bandeau simplifié et afficher « Détecté : … » dans les deux onglets.
        - [x] Modifier les scénarios sous `tests/GWGUI.Tests/Interface/ExplorerViews`, `tests/GWGUI.Tests/Interface/VisualizerViews` et `tests/GWGUI.Tests/Interface/Localization` pour vérifier l'absence des trois libellés redondants, le résumé détecté commun et le retour à la ligne réparé. Validation du 12 septembre 2026 : 114/114 scénarios Visualisateur, Explorateur et localisation réussis, dont les quatre mises en page longues précédemment en échec.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats des scénarios et du build Debug officiel, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : 114/114 scénarios Visualisateur, Explorateur et localisation réussis ; les quatre échecs de mise en page longue sont réparés ; build Debug officiel réussi et exécutable vérifié à `build/Debug/GW GUI/gwgui.exe`.
      - [x] Masquer le libellé de présentation lorsque l'Explorateur n'a aucun choix de format à proposer
        - [x] Modifier `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml` pour donner au libellé `DetectedFormatLabel` le même état initial masqué que son sélecteur.
        - [x] Modifier `tests/GWGUI.Tests/Interface/ExplorerViews/ExplorerDocumentScenarios.cs` pour vérifier que le libellé et le sélecteur sont masqués à vide, visibles ensemble avec plusieurs formats et masqués ensemble avec un seul format.
        - [x] Modifier `docs/tasks/validation.md` avec le résultat du test ciblé et du build Debug officiel, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : 2/2 exécutions du scénario ciblé réussies ; build Debug officiel réussi ; exécutable vérifié à `build/Debug/GW GUI/gwgui.exe`.
      - [x] Finaliser le message professionnel signalant un format incompatible
        - [x] Modifier `src/GWGUI.App/Resources/00-Base/Explorer.resx` avec le titre « Incompatible format » et le message anglais en deux phrases nommant le fichier `{1}` et le format `{0}`.
        - [x] Modifier les 28 catalogues localisés `Explorer.resx` sous `src/GWGUI.App/Resources` avec les traductions Argos du titre et du message complet, puis vérifier la langue et la présence unique de `{0}` et `{1}` dans chaque message. Les sorties Argos incorrectes en arabe, turc, ukrainien, thaï et chinois traditionnel ont été corrigées avant validation ; les 28 catalogues localisés et la base contiennent les deux phrases et exactement une occurrence de chaque paramètre.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/ExplorerViews` et `tests/GWGUI.Tests/Interface/Localization` pour vérifier le texte complet, ses paramètres et sa résolution dans toutes les langues.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats des tests ciblés et du build Debug officiel, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : 30/30 scénarios ciblés réussis dans les 28 langues ; les 29 catalogues effectifs, base comprise, conservent le saut de ligne et exactement une occurrence de `{0}` et `{1}` ; build Debug officiel réussi ; exécutable vérifié à `build/Debug/GW GUI/gwgui.exe`.
      - [x] Unifier l'échec de format du Visualisateur et restaurer le format automatique initial dans les sélecteurs
        - [x] Modifier `src/GWGUI.App/Services/DiskImages/DiskImageWorkspaceController.cs` pour présenter tout échec d'un choix manuel de format dans le Visualisateur avec la même boîte informative que dans l'Explorateur.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml.cs` pour mémoriser le format principal détecté lors de l'ouverture, le remettre dans les sélecteurs et demander sa nouvelle lecture quand « Détection automatique » est recochée, sans modifier le comportement existant lorsque la case est décochée.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/VisualizerViews` et `tests/GWGUI.Tests/Interface/ExplorerViews` pour vérifier la boîte commune sur un échec quelconque, le retour des sélecteurs au premier format détecté d'une image multiformat, l'absence de relecture au décochage et la demande de relecture au recochage.
        - [x] Modifier `docs/tasks/validation.md` avec les résultats des tests ciblés et du build Debug officiel, puis vérifier `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : 2/2 tests ciblés réussis ; build Debug officiel réussi ; exécutable présent dans `build/Debug/GW GUI/gwgui.exe`.
      - [x] Afficher une disquette 5¼ pouces pour les formats Atari 8 bits
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml.cs` pour accepter l'identifiant du format actif et appliquer une icône 5¼ pouces aux formats `atari.90`, `atari.130` et `atari.180`.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml.cs` et `src/GWGUI.App/Views/Controls/Visualization/VisualizerHeaderSection.xaml.cs` pour transmettre au contrôle commun l'identifiant du format affiché.
        - [x] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/ExplorerViews` et `tests/GWGUI.Tests/Interface/VisualizerViews` pour vérifier l'icône 5¼ pouces d'un document Atari 8 bits dans les deux vues.
        - [x] Modifier `docs/tasks/validation.md` avec le résultat des tests ciblés, sans lancer de build Debug. Validation du 12 septembre 2026 : 2/2 tests ciblés réussis ; aucun build Debug lancé.
      - [x] Centraliser les propriétés physiques de tout le catalogue de formats
        - [x] Modifier `src/GWGUI.Domain/Formats/CapabilityAwareImageFormatCatalog.cs` pour conserver `atari.180` dans le catalogue effectif lorsque les capacités Greaseweazle connues ne l'annoncent pas, puisque ce format est pris en charge directement par la lecture ATR interne.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml.cs` pour appliquer l'icône 5¼ pouces aux formats Atari 8 bits, IBM PC 160/180/320/360/1200 Kio, Commodore 1541/1571, Apple II 113/140 Kio, Apple III 140 Kio, Acorn Atom/BBC DFS et Epson QX-10 du catalogue.
        - [x] Modifier `src/GWGUI.Domain/Formats/ImageFormatModels.cs`, `ImageFormatCatalog.cs` et `CapabilityAwareImageFormatCatalog.cs` pour faire de chaque ligne de tout le catalogue la source commune de l'identifiant, la famille, le nom, les extensions, le caractère courant, le format physique de la disquette et les capacités de lecture et d'écriture physiques ; renseigner explicitement ces paramètres pour tous les formats, puis conserver dans le catalogue d'images ceux qui ne sont pas accessibles physiquement.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml.cs`, `src/GWGUI.App/Views/Controls/Explorer/ExplorerSection.xaml.cs` et `src/GWGUI.App/Views/Controls/Visualization/VisualizerHeaderSection.xaml.cs` pour choisir l'icône depuis le format physique de la ligne du catalogue, sans recopier les identifiants de formats dans le contrôle graphique.
        - [x] Modifier `src/GWGUI.App/Controllers/MainWindow/ReadTabController.cs`, `WriteTabController.cs` et `src/GWGUI.App/Views/Windows/Shell/MainWindow.xaml.cs` pour ne proposer dans les opérations physiques que les formats dont la capacité correspondante vaut `true`.
        - [x] Modifier `src/GWGUI.Domain/Formats/ImageFormatModels.cs` pour conserver les capacités physiques actives par défaut pour les formats construits hors du catalogue intégré, dont les formats de tests et les formats fournis par extension.
        - [x] Modifier les tests ciblés du catalogue de formats et de `MediaDocumentIdentity` pour vérifier la présence de `atari.180` avec des capacités connues et la liste des formats 5¼ pouces.
        - [x] Modifier les scénarios ciblés de Lecture et d'Écriture pour vérifier qu'un format dont la capacité physique vaut `false` n'est pas proposé par les sélecteurs correspondants.
        - [x] Modifier `docs/tasks/validation.md` avec le résultat des tests ciblés, sans lancer de build Debug. Validation du 12 septembre 2026 : 4/4 tests ciblés réussis ; aucun lancement de `scripts/build.ps1`.
        - [x] Modifier `src/GWGUI.Domain/Formats/ImageFormatCatalog.cs` pour retirer la ligne vide finale signalée par `git diff --check`, puis recocher le groupe après cette correction.
      - [x] Généraliser la validation des capacités physiques à tout le catalogue
        - [x] Modifier `tests/GWGUI.Tests/Interface/ReadViews/ReadFormatScenarios.cs` et `tests/GWGUI.Tests/Interface/WriteViews/WriteSourceScenarios.cs` pour vérifier que chaque format proposé possède la capacité physique correspondante et qu'aucun format désactivé du catalogue n'est proposé.
        - [x] Modifier `docs/tasks/validation.md` avec le résultat des deux tests ciblés, sans lancer `scripts/build.ps1`. Validation du 12 septembre 2026 : 2/2 tests ciblés réussis.
      - [x] Produire le build Debug demandé après la centralisation du catalogue
        - [x] Créer ou actualiser les fichiers de sortie sous `build/Debug/GW GUI` avec `scripts/build.ps1 -Configuration Debug`, puis modifier `docs/tasks/validation.md` avec le résultat et la présence de `build/Debug/GW GUI/gwgui.exe`. Validation du 12 septembre 2026 : build Debug réussi ; exécutable présent.
      - [x] Distinguer visuellement les disquettes 3 pouces
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml.cs` pour ajouter une géométrie vectorielle propre aux disquettes 3 pouces et l'utiliser lorsque le catalogue indique `FloppyFormFactor.ThreeInch`.
        - [x] Modifier `tests/GWGUI.Tests/Interface/VisualizerViews/MediaVisualizationLayoutTests.cs` pour vérifier qu'un format 3 pouces du catalogue utilise l'icône dédiée, puis modifier `docs/tasks/validation.md` avec le résultat du test ciblé. Validation du 12 septembre 2026 : 1/1 test ciblé réussi.
      - [x] Sans objet — actualiser le build Debug avec l'icône 3 pouces abandonnée avant intégration
        - [x] Modifier `docs/tasks/validation.md` pour annuler l'actualisation de `build/Debug/GW GUI` après la demande de préparer des PNG séparés à valider.
      - [x] Préparer les icônes de disquettes PNG pour validation
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml.cs` et `tests/GWGUI.Tests/Interface/VisualizerViews/MediaVisualizationLayoutTests.cs` pour retirer la proposition vectorielle 3 pouces qui ne doit pas être intégrée.
        - [x] Créer `artifacts/icon-proposals/floppy-5.25.png`, `floppy-3.5-dd-blue.png`, `floppy-3.5-hd-black.png` et `floppy-8.png` comme quatre propositions distinctes à fond transparent, sans modifier leur utilisation dans l'application.
        - [x] Modifier `docs/tasks/validation.md` pour consigner les quatre fichiers PNG remis à l'utilisateur pour validation. Les quatre fichiers mesurent 1254 × 1254 pixels et leur pixel d'angle est transparent.
      - [x] Corriger les propositions 3½ pouces et ajouter la disquette 3 pouces
        - [x] Modifier `artifacts/icon-proposals/floppy-3.5-dd-blue.png` pour supprimer uniquement le carré incorrect placé en haut à gauche.
        - [x] Modifier `artifacts/icon-proposals/floppy-3.5-hd-black.png` pour conserver les ouvertures carrées, avec à gauche le même volet noir d'écriture que sur la proposition 3½ pouces DD ; proposition validée par l'utilisateur.
        - [x] Créer `artifacts/icon-proposals/floppy-3.png` à partir des quatre photographies fournies par l'utilisateur, avec le boîtier noir vertical, la fenêtre ovale supérieure, le moyeu blanc central, le petit trou rond inférieur, les détails supérieurs et une étiquette vierge, vue de face et sur fond transparent ; proposition validée par l'utilisateur.
        - [x] Modifier `docs/tasks/validation.md` pour consigner la validation des propositions 3½ pouces DD, 3½ pouces HD et 3 pouces.
      - [x] Intégrer les cinq icônes de disquettes validées dans l'application
        - [x] Déplacer `artifacts/icon-proposals/floppy-3.png`, `floppy-3.5-dd-blue.png`, `floppy-3.5-hd-black.png`, `floppy-5.25.png` et `floppy-8.png` vers `src/GWGUI.App/Assets/Media` afin d'en faire les ressources de l'application.
        - [x] Modifier `src/GWGUI.Domain/Formats/ImageFormatModels.cs` pour ajouter la densité de disquette au modèle central des formats.
        - [x] Modifier `src/GWGUI.Domain/Formats/ImageFormatCatalog.cs` pour renseigner la densité de chaque format 3½ pouces et permettre le choix de l'icône DD ou HD.
        - [x] Modifier `src/GWGUI.App/GWGUI.App.csproj` pour embarquer les cinq PNG de `Assets/Media` comme ressources WPF.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml` et `MediaDocumentIdentity.xaml.cs` pour afficher le PNG correspondant aux formats 3 pouces, 3½ pouces DD, 3½ pouces HD ou ED, 5¼ pouces et 8 pouces.
        - [x] Modifier `tests/GWGUI.Tests/Interface/VisualizerViews/MediaVisualizationLayoutTests.cs` pour vérifier le choix des cinq présentations dans le bandeau commun.
        - [x] Modifier `tests/GWGUI.Tests/Media/ImageRecognition/CapabilitiesScenarios.cs` pour vérifier les densités renseignées dans le catalogue.
        - [x] Modifier `docs/tasks/validation.md` avec le résultat des tests ciblés et du build Debug de l'application intégrant les PNG : 2 tests ciblés réussis sur 2 ; `scripts/build.ps1 -Configuration Debug` réussi ; exécutable produit dans `build/Debug/GW GUI/gwgui.exe`.
      - [x] Ajouter la présentation 3½ pouces ED et classer les formats MSX
        - [x] Créer `artifacts/icon-proposals/floppy-3.5-ed-black.png` à partir de la 3½ pouces HD validée, avec le marquage `ED` en haut à droite et l'ouverture carrée inférieure droite légèrement remontée.
        - [x] Modifier `src/GWGUI.Domain/Formats/ImageFormatCatalog.cs` pour classer `msx.1d`, `msx.1dd`, `msx.2d` et `msx.2dd` comme disquettes 3½ pouces DD.
        - [x] Modifier `tests/GWGUI.Tests/Media/ImageRecognition/CapabilitiesScenarios.cs` pour vérifier le facteur de forme et la densité des quatre formats MSX.
        - [x] Modifier `docs/tasks/validation.md` pour consigner le résultat du test ciblé et la remise de la proposition ED à l'utilisateur pour validation : test du catalogue MSX réussi, 1 sur 1.
        - [x] Déplacer `artifacts/icon-proposals/floppy-3.5-ed-black.png` vers `src/GWGUI.App/Assets/Media/floppy-3.5-ed-black.png` après sa validation par l'utilisateur.
        - [x] Modifier `src/GWGUI.App/Views/Controls/Common/MediaDocumentIdentity.xaml.cs` pour utiliser l'image ED validée avec les formats `FloppyDensity.ExtendedDensity`.
        - [x] Modifier `tests/GWGUI.Tests/Interface/VisualizerViews/MediaVisualizationLayoutTests.cs` pour vérifier que le format IBM 2,88 Mio sélectionne l'image ED.
        - [x] Modifier tous les fichiers `src/GWGUI.App/Assets/**/*.png` dépassant 300 Ko afin de ramener chacun à 300 Ko maximum en conservant son canal alpha.
        - [x] Modifier `docs/tasks/validation.md` pour consigner le nombre de PNG optimisés, leur taille maximale finale, la conservation de leurs canaux alpha, le résultat des tests ciblés et du build Debug : 68 PNG optimisés sur 68 ; aucun fichier au-dessus de 300 000 octets ; taille maximale finale de 293 192 octets ; 68 images valides en `Format32bppArgb` avec un angle transparent ; 2 tests ciblés réussis sur 2 ; `scripts/build.ps1 -Configuration Debug` réussi ; exécutable vérifié dans `build/Debug/GW GUI/gwgui.exe`.
      - [ ] Affiner ultérieurement les icônes et types de fichiers pour toutes les familles explorées
        - [ ] Modifier `src/GWGUI.App/Dictionaries/Explorer/ExplorerFileTypeProfileCatalog.cs` pour structurer, par famille partageant les mêmes conventions, les extensions connues, le libellé de type, la catégorie d'icône et les métadonnées utiles ; afficher `Fichier <EXT>` pour une extension sans type spécial connu.
        - [ ] Modifier `src/GWGUI.App/Functions/Explorer/ExplorerFileContentClassifier.cs` et `ExplorerFileIconClassifier.cs` afin d'utiliser le catalogue pour les icônes et libellés ; réserver l'identification par signature binaire à une étape ultérieure et ne jamais transmettre ces résultats à la reconnaissance du format du média.
        - [ ] Modifier les ressources communes et traduites sous `src/GWGUI.App/Resources` afin de fournir les libellés de types supplémentaires dans toutes les langues sans dupliquer les valeurs invariantes.
        - [ ] Modifier les scénarios ciblés sous `tests/GWGUI.Tests/Interface/ExplorerViews` pour vérifier les icônes et types de chaque famille ainsi que l'absence d'effet du catalogue d'extensions sur la détection des formats.
      - [ ] Corriger les lecteurs, décodeurs et règles de classement à partir des échecs reproductibles de `F:\Rétro`, sans modifier les images sources
        - [ ] Créer le lecteur de système de fichiers ACIDOS et son test ciblé sous `src/GWGUI.MediaEngine/FileSystems` et `tests/GWGUI.Tests/Media/FileSystems`, puis modifier les constantes, catalogues, compositions et `tests/GWGUI.LocalDiskImageTests/MediaCorpusAuditTests.cs` pour nommer le format physique et restituer les fichiers et dossiers de l'image TeleDisk ACIDOS au lieu de la classer par défaut comme UCSD IBM MFM.
      - [ ] Modifier `tests/GWGUI.LocalDiskImageTests/MediaCorpusAuditTests.cs` avec des assertions autonomes couvrant les familles de disquettes réparées et les transitions successives de formats dans le Visualiseur.
    - [ ] Auditer en lecture seule les corpus locaux de CD et d'images de disques durs
      - [ ] Modifier `tests/GWGUI.LocalDiskImageTests/MediaCorpusAuditTests.cs` pour analyser directement les CD sous `F:\86Box\Isos` et les images de disques durs sous `C:\Users\overt\Documents\GW GUI\Emulation\HDD` et `C:\Users\overt\86Box VMs`, sans copie ni modification des sources.
      - [ ] Modifier les lecteurs et fournisseurs de visualisation optique ou bloc concernés sous `src/GWGUI.MediaEngine` et `src/GWGUI.App` à partir des échecs reproductibles des corpus CD/HDD.
      - [ ] Modifier `tests/GWGUI.LocalDiskImageTests/MediaCorpusAuditTests.cs` avec les assertions autonomes des formats CD/HDD corrigés et la validation de leur affichage dans le Visualiseur.
  - [ ] 1.4 Faire exécuter et exploiter manuellement l'audit local
    - [ ] Modifier `docs/project/testing.md` avec la commande ciblant `MediaCorpusAuditTests`, les racines réellement utilisées, le nombre d'images examinées et les totaux de reconnaissance, visualisation, exploration, formats non pris en charge et erreurs lus dans `build/validation/media-corpus-audit.json`.
    - [ ] Modifier `docs/reference/media-formats.md` avec les résultats réels regroupés par format et famille de machines, en distinguant reconnaissance, représentation Flux ou Sectors, Visualisateur, système de fichiers exploré, absence normale de système de fichiers et erreur à corriger.
    - [ ] Modifier `docs/tasks/validation.md` avant toute correction découverte pour ajouter, après la dernière action cochée et avant la reprise de l'audit, une action concrète par fichier de code, traduction ou test à modifier avec le défaut exact observé.
  - [ ] 1.6 Supprimer le test et le rapport temporaires après exploitation
    - [ ] Supprimer `tests/GWGUI.LocalDiskImageTests/MediaCorpusAuditTests.cs` après consignation de tous les résultats.
    - [ ] Supprimer `build/validation/media-corpus-audit.json` et `build/validation/media-corpus-audit.checkpoint.jsonl` après consignation de tous les résultats.
    - [ ] Modifier `docs/project/testing.md` pour indiquer que l'audit temporaire et ses rapports ont été retirés après validation et que `GWGUI.LocalDiskImageTests` reste hors de la solution et des publications.

- [ ] 2. Refaire les essais avec Greaseweazle
  - [ ] 2.1 Valider les opérations physiques disponibles
    - [ ] Modifier `docs/project/testing.md` après les essais réels de Lecture, Écriture sur support sacrifiable, relecture, conversion, Visualisateur, Explorateur et Effacement pour consigner le matériel, les médias et les résultats.
  - [ ] 2.2 Valider plusieurs contrôleurs lorsque le matériel existe
    - [ ] Modifier `docs/project/testing.md` après un essai avec plusieurs Greaseweazle et lecteurs pour consigner leur détection, leur sélection et les commandes produites.

- [ ] 3. Valider progressivement les entrées et sorties physiques internes
  - [ ] 3.1 Raccorder et vérifier l'Écriture interne
    - [ ] Modifier `src/GWGUI.App/Services/PhysicalDiskWriting/PhysicalDiskWriteService.cs` pour placer l'Écriture interne derrière l'option explicite existante et conserver le repli `gw.exe` pour toute famille qui n'a pas encore été validée manuellement.
    - [ ] Modifier `docs/reference/media-formats.md` après les essais Amiga, Atari ST, IBM, MSX, Apple, Commodore, Acorn/BBC, Amstrad, Epson et DEC pour indiquer famille par famille si le repli `gw.exe` peut être retiré.
  - [ ] 3.2 Raccorder et vérifier la Lecture interne
    - [ ] Modifier `src/GWGUI.App/Services/PhysicalDiskReading/PhysicalDiskReadService.cs` pour placer la Lecture interne derrière l'option explicite existante et conserver le repli `gw.exe` tant que les validations manuelles ne sont pas terminées.
    - [ ] Modifier `docs/reference/media-formats.md` après vérification du checksum SCP, des révolutions, pistes, décodages, annulations et reprises pour indiquer précisément les cas où le repli `gw.exe` peut être retiré.
